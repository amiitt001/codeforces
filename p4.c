#include <stdio.h>
int main()
{
    char op;
    int i, j, x = 0;
    int n;
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
    {
        printf("x");
    }
    for (j = 1; j <= i; j++)
    {
        scanf("%c",&op);
    }
    return 0;
}